# 1. 链接脚本（Linker Script）

GNU `ld` 通过 `.ld` / `.lds` 链接脚本描述 **MEMORY 布局** 与 **SECTIONS 排布**，决定 ELF 各段的 VMA/LMA、入口符号及段与物理存储的映射。编译链末段：`gcc/g++` 调用 `ld`；裸机/嵌入式几乎必写自定义脚本；Linux 用户态通常用默认脚本，交叉编译与特殊内存布局时需显式 `-T`。

> 本文依据 [Using LD, the GNU linker](https://ftp.gnu.org/old-gnu/Manuals/ld-2.9.1/html_mono/ld.html)（GNU ld 手册）整理为中文教程，语法与 modern binutils 仍兼容；具体选项以本机 `ld --help` 为准。

## 【核心精简提纲】

### 记忆主干

**链接末步**：`.o`/`.a` → `ld`（+ 脚本）→ 可执行/`.so`/ROM 镜像。**SECTIONS** 必选（有意义脚本必有）；**MEMORY** 可选（裸机/Flash+RAM 必用）。**`.`** 为位置计数器；**VMA** 运行地址，**LMA** 加载地址（`AT()` 分离，初始化代码负责拷贝）。入口优先级：`-e` > `ENTRY()` > 符号 `start` > `.text` 首字节 > `0`。

### 核心要点清单

- 驱动：`-T script.ld` / `-Wl,-T,script.ld`；**替换**默认脚本（非追加）；隐式脚本仅 `INPUT`/`GROUP` 可追加
- 骨架：`ENTRY(sym)` · `MEMORY { rom(rx): ORIGIN=0, LENGTH=256K … }` · `SECTIONS { .text : { *(.text) } >rom }`
- 收集输入：`*(.text)` 所有文件的 `.text`；`file.o(.data)` 指定文件；`/DISCARD/` 丢弃段
- 段属性：`start` 强制 VMA · `AT(ldaddr)` 设 LMA · `(NOLOAD)` 不占加载镜像 · `>region` 绑定 MEMORY · `=fill` 填充间隙
- 符号：`symbol = . ;` · `PROVIDE(sym = exp)` 仅在未定义且被引用时提供 · `ABSOLUTE(.)` 段内赋绝对地址
- 内置函数：`ADDR` · `LOADADDR` · `ALIGN` · `SIZEOF` · `DEFINED` · `PROVIDE`
- 常量：`4K`/`1M` 后缀 · 十六进制 `0x` · C 风格表达式与 `/* */` 注释
- 调试：`readelf -l/-S` · `objdump -h` · `ld -Map=out.map` · 对照 [[A-编程语言/01.C语言/2. C语言底层原理|C 语言底层原理]] 中 VMA/LMA
- 与运行时区别：链接脚本管 **链接期** 布局；**`ld-linux.so`** 管运行时 `.so` 加载 → [[C-Linux生态/01.Linux环境/2. 动态库与静态库|动态库与静态库]]

### 关键结论 / 流程

```plain
源文件 -c → .o ─┐
                ├→ ld [-T script.ld] [-e entry] … → ELF（段表 + 符号表）
静态库 .a ──────┘
裸机：ROM 中 .data 的 LMA 在 .text 后，启动代码 memcpy(.data) + 清零 .bss
Linux：默认脚本已含 crt 与 libc；自定义脚本多用于 -nostdlib / 固件 / 内核模块
```

### 构建链定位

| 层级 | 工具 | 本笔记 |
|------|------|--------|
| 编译选项 | [[B-构建与脚本/01.构建工具/1. g++|g++]] | `-Wl,-T`、`-nostdlib`、`-e` |
| 规则编排 | [[B-构建与脚本/01.构建工具/2. makefile|Makefile]] | `LDFLAGS += -T` |
| 跨平台 | [[B-构建与脚本/01.构建工具/3. CMake入门与实践|CMake]] | `target_link_options` / `LINKER:` |
| 符号/ELF | [[A-编程语言/01.C语言/2. C语言底层原理|C 底层原理]] | 符号表、readelf |
| 嵌入式 | [[D-系统与架构/04.MCU嵌入式/2. ARM32内存注入（UART）|ARM32 内存注入]] | NOLOAD 固定 RAM 区 |
| 交叉编译 | [[C-Linux生态/03.Linux开发/2. 交叉编译优化选项|交叉编译优化选项]] | 板级 memory map |

### 交叉引用

- [[B-构建与脚本/01.构建工具/1. g++|g++ 编译与链接]] · [[B-构建与脚本/01.构建工具/2. makefile|Makefile]]
- [[A-编程语言/01.C语言/2. C语言底层原理|C 语言底层原理]] · [[C-Linux生态/01.Linux环境/2. 动态库与静态库|动态库与静态库]]
- [[C-Linux生态/03.Linux开发/2. 交叉编译优化选项|交叉编译优化选项]] · [[D-系统与架构/04.MCU嵌入式/2. ARM32内存注入（UART）|ARM32 内存注入]]

## 【完整拓展详情】

## 1.1 ld 与链接脚本的角色

`ld`（GNU linker）合并多个 **object** 与 **archive**，重定位数据、解析符号引用，产出最终可执行文件或共享库。通常由 `gcc`/`g++` 在最后一步隐式调用：

```bash
gcc -o hello hello.o -lc
# 等价于大致：ld -o hello /lib/crt0.o hello.o -lc -dynamic-linker /lib/ld-linux.so ...
```

当需要 **完全控制** 输出布局（裸机、Bootloader、ROM 镜像、自定义内存映射）时，编写 **Linker Command Language** 脚本，经 `-T` 传给 `ld`。脚本控制：

- 输入文件与格式
- 输出段顺序与地址
- 符号赋值与入口点
- COMMON 块放置

## 1.2 命令行：如何指定脚本

| 选项 | 含义 |
|------|------|
| `-T file.ld` / `--script=file.ld` | 用 `file.ld` **替换**默认链接脚本 |
| `-e sym` / `--entry=sym` | 指定入口符号（优先级最高） |
| `-Map=file.map` | 输出链接 map，便于查段地址与符号 |
| `-L dir` | 脚本 `-T` 相对路径时也在 `-L` 目录中搜索 |
| `-nostdlib` | 不链接标准启动文件与默认库（裸机常用） |

经 GCC 转发：

```bash
gcc -nostdlib -T baremetal.ld -Wl,-Map=firmware.map startup.o main.o -o firmware.elf
# 或
g++ ... -Wl,-T,layout.ld,-Map,app.map
```

**注意**：`-T` 指定的是 **完整** 脚本；若某 `.o` 实为只含 `INPUT`/`GROUP` 的脚本片段，则 **追加** 到主脚本，不能重复出现 `SECTIONS`/`MEMORY`（这类命令全局只能一次）。

## 1.3 脚本基本结构

有意义的脚本至少包含 **`SECTIONS`**；嵌入式通常还有 **`MEMORY`** 与 **`ENTRY`**：

```plain
/* 可选：输出格式 / 入口 */
OUTPUT_FORMAT("elf32-littlearm")
ENTRY(_start)

MEMORY
{
    FLASH (rx)  : ORIGIN = 0x08000000, LENGTH = 512K
    RAM   (rwx) : ORIGIN = 0x20000000, LENGTH = 128K
}

SECTIONS
{
    .text :
    {
        KEEP(*(.vectors))
        *(.text*)
        *(.rodata*)
        _etext = .;
    } > FLASH

    .data : AT (ADDR(.text) + SIZEOF(.text))
    {
        _sdata = .;
        *(.data*)
        _edata = .;
    } > RAM

    .bss (NOLOAD) :
    {
        _sbss = .;
        *(.bss*)
        *(COMMON)
        _ebss = .;
    } > RAM
}
```

注释语法与 C 相同：`/* ... */`。

## 1.4 MEMORY：描述目标机存储

`MEMORY` 块声明 **命名存储区域**（与 C 符号不同命名空间）：

```plain
MEMORY
{
    name [(attr)] : ORIGIN = expr, LENGTH = expr
}
```

- **ORIGIN**（可写 `org`/`o`）：区域起始物理地址，须在分配前可求值为常数
- **LENGTH**（可写 `len`/`l`）：区域字节长度；可用 `256K`、`4M` 等后缀
- **attr**：`R` 只读 · `W` 读写 · `X` 可执行 · `A`/`I`/`L` 已分配/已初始化 · `!` 取反

示例（ROM 放代码/只读数据，RAM 放可写段）：

```plain
MEMORY
{
    rom (rx)  : ORIGIN = 0,       LENGTH = 256K
    ram (!rx) : org = 0x40000000, l = 4M
}
```

`ld` **不会**自动搬段以塞进区域；段通过 `>region` 指定归属，超出 `LENGTH` 则 **报错**。

## 1.5 SECTIONS：输出段定义

### 1.5.1 最小形式

```plain
SECTIONS {
    secname : { contents }
}
```

`secname` 两侧需有空格；`:` 与 `{}` 必填。无内容的输出段 **不会** 生成（便于可选段如 `.foo { *(.foo) }`）。

### 1.5.2 收集输入段

| 写法 | 含义 |
|------|------|
| `* (.text)` | 所有尚未分配的文件中的 `.text` |
| `start.o (.vectors)` | 指定文件的段 |
| `* (.text .text.*)` | 多个段名 |
| `* (COMMON)` | 未初始化 COMMON 块 |
| `/DISCARD/` | 匹配段丢弃，不进最终输出 |

经典三元组：

```plain
SECTIONS {
    .text : { *(.text) }
    .data : { *(.data) }
    .bss  : { *(.bss) *(COMMON) }
}
```

通配符规则类似 shell：`*` `?` `[a-z]`；匹配 **命令行已列出** 的文件（脚本内单独写文件名时 `ld` 会尝试打开）。

### 1.5.3 完整段定义语法

```plain
secname start BLOCK(align) (NOLOAD) : AT (ldaddr)
{
    contents
} > region :phdr = fill
```

除 `secname` 与 `contents` 外均可选：

| 成分 | 作用 |
|------|------|
| `start` | 强制本段 VMA（如 `output 0x40000000:`） |
| `BLOCK(n)` / `ALIGN(n)` | 段起始对齐到 n 字节边界 |
| `(NOLOAD)` | 段进入 ELF 但不加载到内存（BSS、栈堆预留、调试区） |
| `AT (ldaddr)` | LMA；默认 LMA = VMA |
| `> region` | 绑定 `MEMORY` 中的区域 |
| `= 0xFF` / `=fill` | 段内未显式写入的间隙填充值 |

### 1.5.4 位置计数器 `.`

特殊符号 **`.`** 表示当前输出位置计数器，只能在 `SECTIONS` 内使用：

```plain
.text : {
    *(.text)
    . = . + 0x1000;   /* 跳过 1KB 空洞 */
    _gap_end = .;
} = 0xFF              /* 空洞填 0xFF */
```

对 `.` 赋值会 **前移** 计数器制造空洞；**不能**向后移动。

### 1.5.5 VMA 与 LMA

- **VMA**（Virtual Memory Address）：程序 **运行** 时使用的地址（`readelf -S` 的 `Addr`）
- **LMA**（Load Memory Address）：段在 **加载镜像**（Flash/ROM/下载文件）中的地址（`readelf -l` 的 `LOAD` 段）

Flash 驻留、RAM 运行的 `.data` 典型写法：

```plain
.text 0x1000 : { *(.text) _etext = .; }
.mdata 0x2000 :
    AT (ADDR(.text) + SIZEOF(.text))
{
    _data = .;
    *(.data);
    _edata = .;
}
.bss 0x3000 : { _bstart = .; *(.bss) *(COMMON); _bend = .; }
```

启动代码（或 `crt0`）需把 ROM 中 `_etext` 起的数据 **拷贝** 到 `_data`…`_edata`，并 **清零** `_bstart`…`_bend`。这与 [[A-编程语言/01.C语言/2. C语言底层原理|C 语言底层原理]] 中程序启动链衔接。

验证：

```bash
readelf -S firmware.elf    # 看各节 VMA
readelf -l firmware.elf    # 看 LOAD 段 LMA/VMA
objdump -h firmware.elf
```

## 1.6 表达式与符号

表达式语法 **同 C**：整数常量、`+ - * / % & | ^ ~`、括号、三元 `?:`。

**赋值**（须独立一行并以 `;` 结尾）：

```plain
_stack_top = 0x20010000;
.data : { _sdata = .; *(.data); _edata = .; }
```

- 在 `SECTIONS` **外**赋值 → 绝对地址符号
- 在段 **内**赋值 → 相对段基址的可重定位符号
- `ABSOLUTE(.)` 在段内得到绝对地址

**PROVIDE**：仅当符号被引用且未定义时才定义（兼容传统 `etext` 等）：

```plain
PROVIDE(_heap_start = .);
PROVIDE(_heap_end = ORIGIN(RAM) + LENGTH(RAM));
```

常用内置函数：

| 函数 | 含义 |
|------|------|
| `ADDR(section)` | 段 VMA |
| `LOADADDR(section)` | 段 LMA（有 `AT` 时与 VMA 不同） |
| `ALIGN(n)` | 将 `.` 对齐到 n 边界（不改变 `.` 除非用于段起始） |
| `SIZEOF(section)` | 段大小 |
| `DEFINED(sym)` | 符号是否已定义 |
| `MAX(a,b)` / `MIN(a,b)` | 最值 |

`ld` 对表达式 **惰性求值**：依赖段大小的赋值在分配完成后才算。

## 1.7 入口点 ENTRY

```plain
ENTRY(_start)
```

优先级（高到低）：

1. 命令行 `-e symbol`
2. 脚本 `ENTRY(symbol)`
3. 符号 `start`（若存在）
4. `.text` 第一节的首地址
5. 地址 `0`

裸机常 `ENTRY(Reset_Handler)`；Linux 用户态默认 `_start` 在 `crt1.o` 中，见 [[A-编程语言/01.C语言/2. C语言底层原理|自定义入口]]（`-nostartfiles -e`）。

## 1.8 Linux 用户态场景

桌面/服务器程序 **通常不写** 链接脚本：`gcc` 使用工具链自带默认脚本（含 `.interp`、`.dynamic`、`.got` 等）。需要自定义的常见情况：

- `-nostdlib` / `-static` 特殊布局
- 内核模块、Bootloader、initramfs 链接
- 将某段固定到特定 VMA（与 [[C-Linux生态/03.Linux开发/2. 交叉编译优化选项|交叉编译]] 内存规划一致）
- 用 `/DISCARD/` 去掉未用段配合 `--gc-sections`

查看默认脚本：

```bash
ld --verbose 2>&1 | less
# 或
gcc -Wl,--verbose 2>&1 | less
```

最小 **追加** 符号（不能含 `SECTIONS`）的脚本片段示例——强制引用某库：

```plain
/* extra.lds — 由 gcc -Wl,-T,extra.lds 追加时仅适合 INPUT/GROUP */
GROUP( -lgcc -lc -lm )
```

自定义 **完整** 布局时必须 `-T my.ld` 并自行包含 `SECTIONS` 及必要 PHDR/动态段，否则易缺 `.dynamic` 导致无法运行。

## 1.9 MCU / 裸机场景

### 1.9.1 Flash + RAM 标准布局

见 §1.3 骨架：向量表 `KEEP(*(.vectors))` 防止 `--gc-sections` 删掉；`.data` 的 `AT()` 指向 Flash 中紧跟 `.text` 的位置；`.bss` 常用 `(NOLOAD)` 仅占 VMA。

### 1.9.2 NOLOAD 预留 RAM（代码注入 / 缓冲区）

不占用下载镜像，仅在运行时占地址空间，见 [[D-系统与架构/04.MCU嵌入式/2. ARM32内存注入（UART）|ARM32 内存注入]]：

```plain
SECTIONS {
    .exec_ram (NOLOAD) : ALIGN(4096) {
        _exec_start = .;
        . += 4K;
        _exec_end = .;
    } > DRAM
}
```

C 代码中通过 `_exec_start`…`_exec_end` 获得固定 RWX 缓冲区。

### 1.9.3 多段 Overlay（省 RAM）

`OVERLAY` 让多个输出段 **共享同一 VMA**，LMA 在 ROM 中依次排列，运行前由加载器/启动代码切换——适合大型算法分块加载（手册 §Overlays）。

## 1.10 与 gcc / Makefile / CMake 集成

**Makefile**：

```makefile
LDFLAGS += -T $(TOP)/scripts/stm32f4.ld -Wl,-Map=$(BUILD)/firmware.map
$(BUILD)/firmware.elf: $(OBJS) $(LDSCRIPT)
	$(CC) $(OBJS) $(LDFLAGS) -nostdlib -o $@
```

**CMake**：

```cmake
target_link_options(firmware PRIVATE
    "-T${CMAKE_SOURCE_DIR}/linker/stm32f4.ld"
    "LINKER:--Map=${CMAKE_BINARY_DIR}/firmware.map"
)
```

**交叉编译**：使用 `aarch64-linux-gnu-ld` 或 `$(CC) -Wl,-T,...` 确保与 [[B-构建与脚本/01.构建工具/1. g++|g++]] 同一工具链前缀。

## 1.11 调试与验证

```bash
# 链接 map：段、符号、丢弃段
ld -Map=out.map -T app.ld ... -o app.elf

# ELF 段与程序头
readelf -S app.elf
readelf -l app.elf

# 反查 VMA
nm app.elf | grep _etext
objdump -t app.elf | head
```

Map 文件中关注：`Memory Configuration`、`*(.text)` 匹配了哪些 `.o`、LMA/VMA 列。

## 1.12 常见踩坑

| 现象 | 原因 / 对策 |
|------|-------------|
| `region RAM overflowed` | `> RAM` 段总和超过 `MEMORY` 的 `LENGTH`；缩段或扩 RAM |
| `Non constant expression for initial address` | 段起始用了链接时未知大小的表达式 |
| `.data` 运行全零 | 有 `AT()` 但未写启动拷贝；或 LMA 算错 |
| 向量表被 GC 掉 | 对关键段用 `KEEP(*(.isr_vector))` |
| `-T` 后无法动态链接 | 自定义脚本缺 `.dynamic`/`.interp`；用户态慎用完整替换 |
| 脚本当 `.o` 追加却含 `SECTIONS` | 应用 `-T` 完整替换，勿混用 |
| `A-B` vs `A - B` | 前者是符号名，后者是减法；符号名含特殊字符用引号 |
| 与 **动态链接器** 混淆 | 链接脚本 ≠ `ld.so`；运行时库路径见 [[C-Linux生态/01.Linux环境/3. rpath与链接路径|rpath 与链接路径]] |

## 1.13 延伸阅读（手册章节）

GNU ld 手册尚含：`PHDRS`（程序头）、`VERSION`（`.so` 符号版本）、`OVERLAY`、`INPUT`/`GROUP`/`OUTPUT_FORMAT`、`ASSERT`。共享库版本脚本与 [[C-Linux生态/01.Linux环境/2. 动态库与静态库|SONAME/版本管理]] 相关；内核 `vmlinux.lds.S` 属于同类语法的高级应用。
