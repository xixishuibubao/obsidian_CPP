# 1. tar压缩软件

## 1.1 简介

tar是一款打包和压缩解压缩软件，默认的压缩算法是gz
## 1.2 命令字

1. `tar -控制参数(c,r,x,z,v,f) 包名 包内容1 包内容2`
2. 一般使用`czvf`,`xzvf name`,name 是包内容也可以是一个目录。
	- c 创建
	- r 追加
	- x 解压缩
	- z 使用压缩成.gz文件
	- v 详细信息
	- f 指定包名称
	- tf 不解压列出包内容，t是`list`