- 接下来我将使用这个markdown文档来向你表达我的需求，使用加粗的是重点，斜体是次重点，`[ ]`方括号内的是一个整体，其他格式根据内容判断优先级。内嵌代码类型为C++,python，以及txt纯文本。

# 1.场景标签

*备选项*：
	1. C++开发
	2. python脚本开发
	3. AI交互提示词训练
	4. 其他：
**实际选择**：我选择4，我的需求是创建一个windows应用，其主要作用是自动注册网站账号。

# 2.核心目标
**开发[具体工具/模块名称]，实现**[核心功能1]**+**[核心功能2]**，满足**[关键约束，如「Windows 10+兼容」「50并发支持」]

** 3. 核心约束：** - 技术栈：**[如「C++17」「Python 3.9+」「Qt 5.15」]** - 性能指标：**[如「1000文件处理≤3秒」「缓存查询延迟≤1ms」]** - 鲁棒性要求：**[如「valgrind无内存泄漏」「无编译警告」]** - 交付物清单：**[如「源代码+编译脚本+使用文档+测试用例」]** 4. 补充约束：*[如「代码符合谷歌编码规范」「文档含操作截图占位符」「日志记录关键操作」]* 5. 输出要求：[核心内容]按**[如「模块拆分文件」「头文件+.cpp」]** 呈现，[补充内容]含*[如「异常处理说明」「版本兼容检测」]*，整体遵循**Markdown格式** 6. 交互要求：（默认3轮，可指定，如「3轮交互，重点校验功能点与交付物」） 7. 核心功能点： - **[功能1]**：[简要说明，如「读取指定目录下所有文件」] - **[功能2]**：[简要说明，如「支持正则表达式匹配文件名」] - **[功能3]**：[简要说明，如「自定义重命名规则（前缀+序号+后缀）」] 8. 异常场景：**[场景1]**[说明，如「文件不存在→提示用户」]、**[场景2]**[说明，如「正则语法错误→返回错误信息」] ## 实战示例（直接复用） ```markdown # 编程外包AI提示词模板 1. 场景标签：1（C++编码） 2. 核心目标：**开发[Redis缓存适配模块]，实现**[插入/查询/删除]**+**[过期淘汰]**，满足**[Linux系统兼容]** 3. 核心约束：** - 技术栈：**C++17** - 性能指标：**支持50线程并发** - 鲁棒性要求：**valgrind无内存泄漏、线程安全** - 交付物清单：**源代码+CMake编译脚本+Markdown使用文档+多线程测试用例** 4. 补充约束：*代码注释完整、测试用例覆盖异常场景、兼容GCC 12+* 5. 输出要求：[核心内容]按**头文件+实现文件**呈现，[补充内容]含*接口调用示例+常见问题解答*，整体遵循**Markdown格式** 6. 交互要求：3轮交互确认 7. 核心功能点： - **插入**：[支持Key-Value+TTL设置] - **查询**：[返回Value或默认值] - **删除**：[按Key精准删除] - **过期淘汰**：[自动清理过期Key] 8. 异常场景：**重复Key插入**[覆盖原有值并更新TTL]、**查询不存在Key**[返回默认值]、**高并发冲突**[通过分段锁解决] ``` ### 优化说明 1. 符号统一：核心信息用`**加粗**`，补充信息用`*倾斜*`，模块/分类用`[方括号]`，AI无需额外说明即可识别优先级； 2. 适配外包场景：明确拆分「核心约束」「功能点」「异常场景」，避免需求模糊，减少返工； 3. 输入便捷：所有符号（`*`「`[]`」）电脑端直接按键输入，无需复杂操作，兼顾效率与可读性。 需要我针对某类具体外包场景（如Python爬虫、Qt桌面应用）生成专属模板，或调整模板结构吗？