# 1.动态库与静态库

## 1.1 动态导出lib和静态lib

1. 动态库 dll不包含符号表，需要配合lib(内部存有导出符号表)
2. 静态库 lib ,虽然同为lib但是静态库的lib会包含程序实现
3. win对于动态和静态库在编译时依赖的都是lib，编译时甚至可以不存在对应的dll。
## 1.2 加载dll

- dll查找路径顺序:
	1. 当前程序路径 > 系统环境变量PATH > 自定义dll路径。
	2. 自定路径需要配合清单,在VS的链接选项里有设置。