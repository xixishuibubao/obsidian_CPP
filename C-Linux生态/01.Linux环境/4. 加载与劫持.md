# 1. 手动加载动态库

## 【核心精简提纲】

### 记忆主干

**手动加载**：`dlopen` 获句柄 → `dlsym` 取符号 → 调用 → `dlclose` 卸载；C++ 导出须 `extern "C"`，库访问主程序符号需主程序 `-rdynamic`。**劫持**：`LD_PRELOAD` 预加载同名符号覆盖，或 `LD_LIBRARY_PATH` 改变搜索顺序；要求 ABI 一致。

### 核心要点清单

- `dlopen`：相对路径 `./libfoo.so` 有效，`~` 无效；`RTLD_NOW` 立即解析，`RTLD_LOCAL`/`GLOBAL` 控制符号可见性
- `dlerror()`：调用即清除上次错误，失败分支须立即读取
- 生命周期：`constructor`/`destructor` 属性在加载/卸载时自动执行
- `LD_PRELOAD`：冒号分隔多库，左优先；同名符号以先加载为准
- 主程序共享符号给 `.so`：链接加 `-rdynamic`（`-export-dynamic`）
- setuid 程序忽略 `LD_PRELOAD` / `LD_LIBRARY_PATH`

### 关键结论 / 流程

```plain
插件：dlopen → dlsym → 调用 → dlclose
劫持：LD_PRELOAD=libhook.so ./app（ABI + extern "C" 一致）
```

### 交叉引用

- [[2. 动态库与静态库|动态库与静态库]] — `.so` 编译与 SONAME
- [[3. rpath与链接路径|rpath 与链接路径]] — `dlopen` 不搜 rpath 的边界
- [[D-系统与架构/02.软件架构设计/5. 插件架构设计|插件架构设计]] — 工厂模式、热插拔
- [[C-Linux生态/07.Telescope1126实战/4. 驱动插件与pybind11动态加载|驱动插件与 pybind11 动态加载]] — 工程插件实践
- [[B-构建与脚本/01.构建工具/1. g++|g++ 编译选项]] — `-rdynamic`、`-shared`

## 【完整拓展详情】


> 插件架构设计（工厂模式、热插拔、生命周期管理）参见 [[D-系统与架构/02.软件架构设计/5. 插件架构设计|插件架构设计]]

## 1.1 `demo`
### 1.1.1 调用方
```C++
	#include <dlfcn.h>
	#include <iostream>
	void myfunc() {
		void* handle = dlopen("./libmylib.so",RTLD_NOW | RTLD_LOCAL);
		if(!handle) {
			std::cout << dlerror();
			return;
			}
		dlclose(handle);
		unlink("./libmylib.so");
	}
```
1. `void* handle = dlopen(libname，flags)`,加载动态库，返回句柄。
	1. 库名不识别`~/`,但识别相对路径`./`，且为完整库名，不可省略前面的`lib`。
	2. `flags`常用组合：
		1. `RTLD_NOW | RTLD_LOCAL`,立刻加载，只加载库本身符号表，库不可访问原程序内容。
		2. `RTLD_NOW | RTLD_GLOBAL`,立即加载，且共享主程序符号表，库可访问其变量和方法。
		3. **库要访问原程序内容，需要编译的时候添加`-rdynamic`,共享符号表不然库识别不到。**
2. `dlerror()`,会显示库加载失败的原因，返回值为字符串。
	1. `dlerror()`调用后会清除上一次的`errorcode`。
3. `dlclose(handle)`,从程序中卸载库，关闭句柄。
4. `unlink(libname)`,对于一次性库，用完后使用`unlink`删除，其作用等价于`rm libname`。
### 1.1.2 so库
```c++
	/* .so加载时自动执行（constructor属性，无需主程序调用）*/
	__attribute__((constructor)) 
	void init_collect() { }
	/* .so卸载时自动执行（destructor属性，无需主程序调用）*/
	__attribute__((destructor)) 
	void cleanup() { }
```
- 如果原程序需要加载并调用库的函数，要将其使用`extern "C" { }`包裹，避免编译后函数名被`c++`规则混淆，无法正确识别到并加载。

# 2. 劫持动态库
- 劫持要求ABI一致，推荐使用`extern "C" { }`保证注入和劫持函数的ABI一致性。
## 2.1 环境变量劫持
### 2.1.1 `LD_PRELOAD`预加载劫持
- `LD_PRELOAD=liba.so:libb.so ./myexe`,多个库名之间用`:`间隔开。
- 优先级从左到右依次减小，同名库多次劫持以最先加载的为准。
### 2.1.2 `LD_LIBRARY_PATH`,劫持库目录
- `LD_LIBRARY_PATH=/mylib:PATH`，原理和`LD_PRELOAD`相同，先加载的优先级更高。

## 2.2 常见踩坑

| 现象 | 原因 | 处理 |
|------|------|------|
| `dlopen` 返回 NULL | 路径错、缺依赖 `.so`、架构不匹配 | 立即 `dlerror()`；`ldd libfoo.so` 查依赖 |
| `dlsym` 找不到 C++ 函数 | name mangling | 导出函数包 `extern "C"` |
| 插件读不到主程序变量 | `RTLD_LOCAL` 且未 `-rdynamic` | 主程序链接 `-rdynamic` 或 `RTLD_GLOBAL` |
| 劫持无效 | 符号未导出 / ABI 不一致 / setuid 程序 | 确认 `nm -D` 可见性；非 setuid 测试 |
| 卸载后崩溃 | 仍有函数指针指向已卸载代码 | `dlclose` 前确保无残留回调 |

> 搜索路径细节见 [[3. rpath与链接路径|dlopen 搜索路径]]；跨平台对比见 [[D-系统与架构/01.架构体系/2. 跨平台动态库加载机制|跨平台动态库加载机制]]；Valgrind 排查泄漏见 [[C-Linux生态/04.调试与优化/9. Valgrind 内存调试|Valgrind 内存调试]]。
