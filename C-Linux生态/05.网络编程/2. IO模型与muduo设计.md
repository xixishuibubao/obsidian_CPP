# 1. IO 模型与 muduo 设计

Linux 五种 IO 模型辨析、高并发服务器设计要素，以及 muduo 基础模块（Log / InetAddress / Buffer / EventLoop）职责划分。

## 【核心精简提纲】

### 记忆主干

**阻塞 IO** 等数据就绪才返回；**非阻塞** 无数据立刻 `EAGAIN` 需轮询；**IO 复用**（select/poll/epoll）先等多 fd 就绪再读写；**信号驱动** 少用；**真异步**（io_uring）内核完成拷贝。**muduo = Reactor + one loop per thread + epoll**。

### 核心要点清单

- `recv`/`read` 本质相同；`size==0` 对端关闭，`<0`+`EAGAIN` 非阻塞无数据
- epoll：红黑树存 fd + 就绪链表回调，只返回活跃 fd
- Nginx：多进程 + epoll；muduo：多线程每线程一个 EventLoop
- muduo 模块：`Logger` / `Timestamp` / `InetAddress` / `Buffer` / `Channel` / `EventLoop`

### 五种 IO 对比

| 模型 | 等数据 | 拷数据 | 典型 API |
|------|--------|--------|----------|
| 阻塞 | 阻塞 | 阻塞 | `recv` flags=0 |
| 非阻塞 | 立即返回 | 阻塞 | `fcntl O_NONBLOCK` |
| IO 复用 | epoll_wait | 阻塞 | epoll |
| 信号驱动 | 信号通知 | 同步处理 | 少用 |
| 异步 | 内核完成 | 内核完成 | io_uring（5.1+） |

### 交叉引用

- [[C-Linux生态/05.网络编程/1. muduo-linux服务器|Socket / epoll 示例代码]]
- [[C-Linux生态/06.开源项目分析/1. 开源项目分析|Muduo 源码学习资源]]
- [[D-系统与架构/03.并发与内存/3. 线程与异步|线程与 async]]

## 【完整拓展详情】

## 1.1 技术栈分层（速查）

| 层 | 代表 |
|----|------|
| 网络库 | muduo、libevent |
| 缓存 | Redis、Memcached |
| 存储 | MySQL、MongoDB |

> 本节为学习路线索引；深度解析见 [[C-Linux生态/06.开源项目分析/1. 开源项目分析|开源项目分析]]。

## 1.2 IO 阻塞与非阻塞

默认 `recv(..., flags=0)` **阻塞**直到有数据。

```plain
ssize_t recv(int sockfd, void* buf, size_t len, int flags);
```

返回值：

| 返回值 | 含义 |
|--------|------|
| `> 0` | 实际字节数 |
| `== 0` | 对端正常关闭 |
| `< 0` | 错误；`errno==EAGAIN/EWOULDBLOCK` 表示非阻塞下暂无数据 |

## 1.3 五种 IO 模型

`recv` 是 `read` 的 socket 特化，本质都是内核态 IO。

### 1.3.1 阻塞 blocking

- **等数据**：未就绪则线程挂起
- **读写**：就绪后从内核缓冲区拷贝到用户空间（仍可能阻塞）

### 1.3.2 非阻塞 non-blocking

- **等数据**：未就绪立即返回 `EAGAIN`，需应用层轮询
- **读写**：就绪后拷贝；轮询空转会浪费 CPU

### 1.3.3 IO 复用

`select` / `poll` / `epoll` 批量监听 fd 就绪事件；读写本身仍可在阻塞或非阻塞模式下进行。

- 核心：**事件通知**——只处理就绪 fd，避免无效 `recv`
- epoll 内核：fd 注册进红黑树 → 就绪回调链入就绪链表 → `epoll_wait` 取活跃 fd

### 1.3.4 信号驱动

注册 signal handler，数据就绪时内核发信号；handler 内同步读数据。存在信号竞争与多线程安全难题，**服务器后端极少采用**。

### 1.3.5 异步 async

- **Linux aio**（2.6–4.x）：`libaio`，主要 **O_DIRECT 文件 IO**，不支持 socket
- **io_uring**（5.1+，Ubuntu 20.04+）：统一 fd，真·内核异步完成
- 区别于业务层「多线程/async 库」——此处指 **内核级** 异步

### 踩坑

| 易混点 | 澄清 |
|--------|------|
| epoll ET 漏读 | 边缘触发须一次读尽或循环读到 EAGAIN |
| 「异步库」vs io_uring | Node/libuv 多为线程池 + IO 复用，不是内核 aio |
| 非阻塞 + 单线程 | 仍需 epoll 驱动，裸 while 轮询会烧 CPU |

## 1.4 网络服务器设计要素

### 1.4.1 线程 vs 进程

- CPU 调度单位是线程；IO 密集型应多线程提高阻塞时 CPU 利用率
- **多线程**：Web API、DB 访问，逻辑简单，共享内存通信方便
- **多进程（Nginx）**：worker 隔离，单 worker 崩溃不影响其他；配合 epoll/kqueue

### 1.4.2 one loop per thread

每个 IO 线程（或进程/协程）运行一个 **EventLoop**，处理注册在其上的 Channel 事件——muduo、Netty 等主流 Reactor 方案。

### 1.4.3 epoll 数据结构（复习）

1. 注册 fd → 红黑树
2. 就绪事件 → 就绪链表
3. 用户态 `epoll_wait` 批量取回

代码级示例：[[C-Linux生态/05.网络编程/1. muduo-linux服务器#1.5 epoll 服务器（Linux 推荐）|epoll 回显服务器]]。

## 1.5 muduo 基础模块

### 1.5.1 Log 日志模块

- 职责：分级日志接口
- 元素：`LogLevel`（DEBUG/INFO/ERROR/FATAL）、`Timestamp`（`now()` / `toString()`）

### 1.5.2 InetAddress 地址模块

- 职责：封装 `sockaddr_in` / `sockaddr_in6`，字节序转换、IP:Port 解析
- 隐藏 `ipv4` / `ipv6` 差异

### 1.5.3 Buffer 与 EventLoop（核心）

| 类 | 职责 |
|----|------|
| `Buffer` | 预分配 + 读/写指针，减少 `recv` 系统调用次数；支持 `append` / `retrieve` |
| `Channel` | 封装 fd + 关注事件 + 回调；不拥有 fd |
| `EventLoop` | 循环 `poll` → 分发 `Channel` 回调；**one loop per thread** |
| `Poller` | 抽象 epoll（策略模式，可换 poll） |

muduo 设计哲学：够用即可的极简 Reactor，对比 ACE 的过度抽象。详见 [[C-Linux生态/06.开源项目分析/1. 开源项目分析#2.1.1 Muduo（C++，Reactor 模式）|Muduo 架构核心]]。
