# 1. muduo 与 Linux 网络编程

Socket 编程通用流程与 select / poll / epoll 回显示例；muduo 库作为 Reactor 封装的上层入口。

## 【核心精简提纲】

### 记忆主干

**TCP 服务端**：`socket` → `setsockopt(REUSEADDR)` → `bind` → `listen` → 多路复用等待 → `accept` 得 `conn_fd` → `recv`/`send`。**Linux 生产用 epoll**；select 受 `FD_SETSIZE` 限制；muduo 在 epoll 之上封装 EventLoop/Reactor。

### 核心要点清单

- `recv` 返回 0：对端正常关闭；`<0` 且 `EAGAIN`：非阻塞暂无数据
- select：每次 `FD_ZERO` + 遍历 fd，O(n)；poll：fd 数组，无 1024 硬限
- epoll：`epoll_create1` → `EPOLL_CTL_ADD` → `epoll_wait` 只返回就绪 fd
- 示例均为阻塞 + 单线程回显，真实服务需非阻塞 + 连接池/线程池

### 关键流程

```plain
listen_fd ──epoll_ctl ADD──► epoll_fd
                │
         epoll_wait 就绪
                ├─ listen_fd 可读 → accept → EPOLL_CTL_ADD conn_fd
                └─ conn_fd 可读 → recv；n<=0 则 close + EPOLL_CTL_DEL
```

### 交叉引用

- [[C-Linux生态/05.网络编程/2. IO模型与muduo设计|IO 模型与 muduo 模块设计]]
- [[C-Linux生态/06.开源项目分析/1. 开源项目分析|Muduo 源码解析资源]]
- [[C-Linux生态/06.开源项目分析/2. 服务器开源项目|服务器框架对比]]

## 【完整拓展详情】

## 1.1 通用流程（阻塞单连接）

```cpp
int listen_fd = socket(AF_INET, SOCK_STREAM, 0);

int opt = 1;
setsockopt(listen_fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

struct sockaddr_in serv_addr{};
serv_addr.sin_family = AF_INET;
serv_addr.sin_addr.s_addr = INADDR_ANY;
serv_addr.sin_port = htons(PORT);
bind(listen_fd, (struct sockaddr*)&serv_addr, sizeof(serv_addr));
listen(listen_fd, SOMAXCONN);

struct sockaddr_in cli_addr{};
socklen_t cli_len = sizeof(cli_addr);
int conn_fd = accept(listen_fd, (struct sockaddr*)&cli_addr, &cli_len);

char buf[1024];
ssize_t n = recv(conn_fd, buf, sizeof(buf) - 1, 0);
if (n <= 0) { close(conn_fd); /* 断开 */ }
```

> 开发调试可开 `SO_REUSEADDR` 避免 `TIME_WAIT` 占端口；生产高并发还需 `SO_REUSEPORT`、非阻塞、`TCP_NODELAY` 等，见 [[C-Linux生态/05.网络编程/2. IO模型与muduo设计|IO 模型]]。

## 1.2 客户端

完整可编译示例：命令行传入 IP/端口，`connect` 后循环 `send`/`recv` 回显，输入 `q` 退出（原笔记保留完整代码结构，要点为 `inet_pton` 解析地址、`fgets` 去换行）。

## 1.3 select 服务器（跨平台）

- 每次循环 `FD_ZERO` 再 `FD_SET` 所有 fd——**O(n) 且易错**
- `FD_SETSIZE` 通常 1024，不适合万级连接
- 新连接：`accept` 后更新 `max_fd`

## 1.4 poll 服务器

- `struct pollfd fds[]` 管理 fd，`revents & POLLIN` 判断可读
- 客户端断开：用末尾元素覆盖当前槽位，`nfds--`（**紧凑数组**技巧）

## 1.5 epoll 服务器（Linux 推荐）

```cpp
int epoll_fd = epoll_create1(0);
struct epoll_event ev{};
ev.events = EPOLLIN;
ev.data.fd = listen_fd;
epoll_ctl(epoll_fd, EPOLL_CTL_ADD, listen_fd, &ev);

struct epoll_event events[MAX_EVENTS];
int nfds = epoll_wait(epoll_fd, events, MAX_EVENTS, -1);
for (int i = 0; i < nfds; ++i) {
    if (events[i].data.fd == listen_fd) { /* accept + EPOLL_CTL_ADD */ }
    else { /* recv；n<=0 → close + EPOLL_CTL_DEL */ }
}
```

### 踩坑

| 问题 | 说明 |
|------|------|
| 单线程阻塞 `recv` | 一个慢客户端拖死全体；改非阻塞 + epoll ET/LT |
| 忘记 `EPOLL_CTL_DEL` | fd close 后 epoll 仍可能报告事件（取决于 LT/ET） |
| `listen backlog` 过小 | 高并发 SYN 队列溢出，适当调大 `listen` 第二参数 |
| 示例里变量名不一致 | 教学代码 `close(fd)` 应为 `conn_fd`，复制时注意 |

## 1.6 muduo 网络库（上层）

Muduo 是基于 **Reactor** 的 C++ 网络库（陈硕），在 epoll 之上提供 `EventLoop`、`Channel`、`Buffer`、`TcpServer` 等，约 5000 行核心代码实现高并发。

- 书籍：《Linux 多线程服务器编程：使用 Muduo C++ 网络库》
- 源码：[chenshuo/muduo](https://github.com/chenshuo/muduo)
- 模块设计与 IO 模型辨析：[[C-Linux生态/05.网络编程/2. IO模型与muduo设计|IO 模型与 muduo 设计]]
