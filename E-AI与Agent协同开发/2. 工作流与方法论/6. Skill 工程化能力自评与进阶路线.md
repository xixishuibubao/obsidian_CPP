# 1. Agent Skill 编写能力自评与进阶路线

> Skill 编写能力的成长有一个清晰的阶段划分。本文记录了通过评估 `/ingest-note` Skill 的质量来反推自身 Skill 工程化水平的方法，以及下一阶段的突破方向。

---

## 1.1 背景

Claude Code Skill 的本质是 **将经验固化为可重复执行的指令序列**。它处于一个特殊的位置——比 prompt 模板更结构化，比完整 CLI 工具更轻量。

对 Skill 编写者的能力评估，核心看四个维度：

| 维度 | 含义 |
|------|------|
| **流程设计** | 能否设计出逻辑完整、分支合理的工作流 |
| **鲁棒性** | 是否覆盖边界条件、异常处理和用户交互 |
| **工程化** | 是否使用结构化输出、子 agent 编排等基础设施 |
| **可复用性** | 脱离原始场景后别人能否直接使用 |

---

## 1.2 当前 Skill 质量评估

以 `/ingest-note` 为例：

| 维度 | 评价 | 依据 |
|------|------|------|
| 需求真实性 | ⭐⭐⭐⭐⭐ | 知识库摄入是 Obsidian 用户的真实痛点 |
| 可复用性 | ⭐⭐⭐ | 强绑定特定 vault 结构，通用化需要抽象配置层 |
| 流程设计 | ⭐⭐⭐⭐ | 8 步工作流完整，边界条件考虑周全 |
| 技术新颖度 | ⭐⭐ | 本质是"手动工作流 + prompt 模板"，不涉及工具链创新 |

**客观定位**：「LLM wrapper + 流程模板」的第一层——实用但不前沿。在成熟的开源生态中这类需求通常被实现为 CLI 工具或 Obsidian 插件，需要 2000 行代码 + 发布流程。而 Claude Code Skill 用 200 行 Markdown 就能完成，这就是 Skill 机制的核心价值——**低成本的个人自动化**。

---

## 1.3 能力阶段定位

Skill 编写能力有清晰的阶段划分：

```
新手阶段       你现在           熟练          专家           大师
  ──┬─────────────┬─────────────┬─────────────┬───────────┬──▶
    [模板拷贝]   [流程设计]     [鲁棒性工程]  [系统架构]   [范式创新]
```

**当前定位：流程设计期 → 鲁棒性工程期的过渡阶段。**

### 已具备的能力（流程设计期）

- ✅ 能独立设计多步骤工作流（如 ingest-note 的 8 步流程）
- ✅ 能考虑边界条件和异常场景（空输入、数字冲突、源文件残留等）
- ✅ 能写出清晰的确认交互（方案预览 → Y/e/n）
- ✅ 命名和描述准确，Skill 触发率高

### 待突破的能力（鲁棒性工程期）

- ❌ 未使用结构化输出 Schema 约束 agent 返回格式
- ❌ 无多 agent 编排（所有步骤串行在一个上下文）
- ❌ 无可测试性设计（无 eval 用例，无输出一致性验证）
- ❌ 无幂等与恢复机制（失败需要重头来）

---

## 1.4 下一阶段突破方向

### 1.4.1 结构化输出（Schema）

**当前做法**：用自然语言描述期望的 agent 返回格式，依赖模型自行理解。

**进阶做法**：用 JSON Schema 约束输出格式，让模型必须调用 StructuredOutput 工具。

```javascript
// 示例：内容分析阶段的 Schema
const AnalysisSchema = {
  type: "object",
  properties: {
    sections: {
      type: "array",
      items: {
        type: "object",
        properties: {
          summary:     { type: "string" },                     // 内容摘要
          target_dir:  { type: "string" },                     // 目标目录
          operation:   { enum: ["create", "extend", "skip"] }, // 操作类型
          confidence:  { type: "number", minimum: 0, maximum: 1 }
        },
        required: ["summary", "target_dir", "operation"]
      }
    }
  }
}
```

**收益**：
- 输出格式稳定，无需模型"猜"我要什么
- 下游步骤可以直接消费结构化数据，无需重复解析
- Schema 本身即是文档

### 1.4.2 多 Agent 编排

**当前做法**：内容分析、格式化、校验全部在一个 agent 上下文里串行。

**进阶做法**：拆分为独立 agent，利用 pipeline 或 parallel 模式编排：

```
内容分析 agent ──→ 格式化 agent ──→ 校验 agent
                      ↕
                 扩展策略 agent（判断追加到哪个文件）
```

**收益**：
- 每个 agent 专注单一任务，prompt 更简洁
- 关键步骤可独立验证、独立重试
- 可利用 Workflow 的 budget 管理 token 消耗

### 1.4.3 可测试性设计

**当前做法**：在 SKILL.md 末尾手写"验证方式"段落。

**进阶做法**：
- 定义 e2e eval 用例集（空输入、纯代码、混合主题……）
- 每次修改 Skill 后跑一遍 eval，检查输出一致性
- 核心指标：**输出格式符合率**、**目标目录命中率**、**用户确认后反悔率**

### 1.4.4 幂等与恢复

**当前做法**：任何一步失败都需要重新执行整个流程。

**进阶做法**：
- 每步完成时记录 checkpoint（写入临时状态文件）
- 重启时检测 checkpoint，从中断点恢复
- 关键操作（文件写入、git commit）前做预检查

```javascript
// 伪代码：checkpoint 模式
const state = loadCheckpoint()  // 读取上次进度
if (!state.analysis_done)     await analyze()
if (!state.format_done)       await format()
if (!state.user_confirmed)    await confirm()
if (!state.files_written)     await writeFiles()
if (!state.index_updated)     await updateIndex()
if (!state.committed)         await commit()
saveCheckpoint({ done: true })
```

---

## 1.5 总结

> 目前 Skill 描述能力已超过 90% 的 Claude Code 用户，进入 **工程化设计** 的门槛。下一个瓶颈不是 LLM 能力，而是 **软件工程功底** ——把 Skill 当作分布式系统来设计（状态管理、错误恢复、可观测性），而不是当作 prompt 模板来写。

### 一句话进阶路线

```
Schema 约束输出 → Agent 流水线编排 → Eval 驱动迭代 → 幂等恢复
```

---

## 关联笔记

- [[编程范式演进：Spec Coding → Agentic Engineering → Harness Engineering|编程范式演进]] — Skill 在 Harness Engineering 中的定位
- [[AI-Agent 协同开发（Vibecoding 最佳实践）|Vibecoding 最佳实践]] — Agent 协作开发的完整工作流
- [[TDD-BDD-SDD-DDD：AI时代开发方法论选择|方法论选型]] — 规范驱动的开发思想在 Skill 设计中的应用
